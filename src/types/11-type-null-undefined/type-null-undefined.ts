// type null é a ausência de valor
let nome: null; //uma vez atribuida não pode ser alterada,

let nome2: string | null; //normalmente vai ser utilizada com o union type!
nome2 = null;

//ja o undefined -> não foi atribuido algum valor ou tipo!
let nomeUser;
console.log(nomeUser);

